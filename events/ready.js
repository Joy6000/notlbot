module.exports = async (client, handler) => {
    // let reactions = [
    //     "👍",
    //     "😣",
    //     "😰"
    // ]
    // const assets = require('../utils/assets.json')
    // let channel = client.channels.cache.get('811688285315006464')
    // const message = await channel.send('react')
    // reactions.forEach(react => message.react(react))
    // client.on('messageReactionAdd', (msg, person) => {
    //     if (person.bot) return
    //     if (msg._emoji.name === '👍') {
    //         client 
    //     }
    // })

}